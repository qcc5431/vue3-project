# Vue3 笔记社交平台 - 完整设计文档

## 📋 项目概述

本文档记录了 Vue3 笔记社交平台的完整设计与实施流程，包括所有功能模块的架构设计和技术实现。

**项目定位**：类小红书的笔记社交平台

**核心功能模块**：

- 📝 **笔记管理**：创建、编辑、删除、详情、列表展示
- 🖼️ **多媒体封面**：支持多图/视频，混合模式（手动+自动提取）
- ✍️ **富文本编辑**：增强版 Markdown 编辑器，表情选择器
- 📱 **视图模式**：走马灯卡片、瀑布流布局、视图动态切换
- 👥 **社交互动**：关注/取消关注、点赞、收藏、评论（含评论点赞）
- 🔐 **用户体系**：注册、登录（用户名/邮箱/手机号多模式）、个人信息
- 📂 **分类管理**：侧边栏分类导航、收藏夹管理

**最后更新时间**：2026-03-03

---

## 🛠️ 技术栈

### 核心框架

| 技术       | 版本   | 说明                            |
| ---------- | ------ | ------------------------------- |
| Vue        | 3.5.22 | 渐进式 JavaScript 框架          |
| TypeScript | 5.9.0  | JavaScript 的超集，提供类型安全 |
| Vite       | 7.1.11 | 下一代前端构建工具              |
| Pinia      | 3.0.4  | Vue3 官方状态管理库             |
| Vue Router | 4.6.4  | Vue.js 官方路由管理器           |

### UI 与组件库

| 技术          | 版本   | 说明         |
| ------------- | ------ | ------------ |
| Element Plus  | 2.13.0 | Vue3 组件库  |
| vuedraggable  | 4.1.0  | 拖拽排序组件 |
| sass-embedded | 1.97.2 | CSS 预处理器 |

### 网络与工具

| 技术                    | 版本   | 说明         |
| ----------------------- | ------ | ------------ |
| Axios                   | 1.13.2 | HTTP 请求库  |
| unplugin-auto-import    | 20.3.0 | API 自动导入 |
| unplugin-vue-components | 30.0.0 | 组件自动导入 |

### 开发工具

| 技术     | 版本   | 说明                    |
| -------- | ------ | ----------------------- |
| ESLint   | 9.39.1 | 代码质量检查            |
| Prettier | 3.6.2  | 代码格式化              |
| vue-tsc  | 3.1.1  | Vue TypeScript 类型检查 |

---

## 📊 系统架构

### 整体功能架构

```mermaid
graph TB
    A[Vue3笔记社交平台] --> B[笔记管理]
    A --> C[社交互动]
    A --> D[用户体系]
    A --> E[视图模式]

    B --> B1[创建笔记]
    B --> B2[笔记列表]
    B --> B3[笔记详情]
    B --> B4[编辑笔记]
    B --> B5[删除笔记]

    C --> C1[关注用户]
    C --> C2[点赞笔记]
    C --> C3[收藏笔记]
    C --> C4[发表评论]
    C --> C5[评论点赞]

    D --> D1[用户注册]
    D --> D2[用户登录]
    D --> D3[个人信息]
    D --> D4[我的笔记]
    D --> D5[我的关注]
    D --> D6[我的收藏]

    E --> E1[卡片走马灯视图]
    E --> E2[卡片瀑布流视图]
    E --> E3[视图动态切换]
```

### 项目目录结构

```
src/
├── api/                    # API 接口层
│   ├── types/             # 类型定义
│   │   ├── note.ts        # 笔记相关类型
│   │   ├── user.ts        # 用户相关类型
│   │   ├── social.ts      # 社交相关类型
│   │   └── folder.ts      # 文件夹相关类型
│   ├── note.ts            # 笔记 API
│   ├── user.ts            # 用户 API
│   ├── social.ts          # 社交 API
│   ├── folder.ts          # 文件夹 API
│   └── upload.ts          # 上传 API
│
├── assets/                # 静态资源
│   ├── images/           # 图片资源
│   └── styles/           # 样式文件
│       ├── variables.scss # 变量定义
│       ├── global.scss    # 全局样式
│       └── index.scss     # 样式入口
│
├── components/            # 公共组件
│   ├── AuthModal.vue      # 登录/注册弹窗
│   ├── NoteCard.vue       # 笔记卡片
│   ├── CarouselNoteCard.vue # 走马灯卡片
│   ├── WaterfallList.vue  # 瀑布流组件
│   ├── MediaCarousel.vue  # 媒体轮播
│   ├── VideoPlayer.vue    # 视频播放器
│   ├── MarkdownEditor.vue # Markdown编辑器
│   ├── MarkdownPreview.vue # Markdown预览
│   ├── EmojiPicker.vue    # 表情选择器
│   ├── CommentList.vue    # 评论列表
│   ├── CommentInput.vue   # 评论输入
│   ├── UserAvatar.vue     # 用户头像
│   ├── MediaUploader.vue  # 媒体上传
│   └── BackToTop.vue      # 返回顶部
│
├── layout/                # 布局组件
│   ├── index.vue          # 主布局
│   └── components/
│       ├── Header.vue     # 顶部导航
│       └── CategorySidebar.vue # 侧边栏
│
├── router/                # 路由配置
│   └── index.ts
│
├── store/                 # 状态管理
│   ├── index.ts           # Pinia 根配置
│   └── modules/
│       ├── user.ts        # 用户状态
│       ├── note.ts        # 笔记状态
│       ├── social.ts      # 社交状态
│       └── folder.ts      # 文件夹状态
│
├── utils/                 # 工具函数
│   ├── request.ts         # Axios 封装
│   ├── mediaHelper.ts     # 媒体处理
│   └── markdownConverter.ts # Markdown转换
│
├── views/                 # 页面组件
│   ├── home/             # 首页
│   ├── note/             # 笔记相关
│   │   ├── create.vue    # 创建/编辑
│   │   └── detail.vue    # 详情
│   ├── my-notes/         # 我的笔记
│   ├── favorites/        # 我的收藏
│   ├── following/        # 我的关注
│   ├── profile/          # 个人主页
│   └── 404/              # 404页面
│
├── mock/                  # Mock数据
│   ├── notes.ts
│   ├── users.ts
│   ├── comments.ts
│   └── folders.ts
│
├── App.vue               # 根组件
└── main.ts               # 入口文件
```

### 页面路由结构

```
/                      - 首页（走马灯+瀑布流）
├─ /following          - 我的关注（需登录）
├─ /favorites          - 我的收藏（需登录）
├─ /my-notes           - 我的笔记（需登录）
├─ /create             - 创建笔记（需登录）
├─ /note/:id           - 笔记详情
├─ /note/:id/edit      - 编辑笔记（需登录）
├─ /profile/:id?       - 个人主页
└─ /404                - 404页面
```

### 状态管理架构

```
store/
├─ index.ts              - Pinia 根配置
└─ modules/
    ├─ user.ts           - 用户状态
    │   ├─ token         - 登录令牌
    │   ├─ userInfo      - 用户信息
    │   ├─ authModalVisible - 登录弹窗状态
    │   └─ authModalType - 弹窗类型（login/register）
    │
    ├─ note.ts           - 笔记状态
    │   ├─ notes         - 笔记列表
    │   ├─ currentNote   - 当前笔记
    │   ├─ total         - 总数
    │   ├─ page          - 当前页
    │   ├─ sortType      - 排序类型
    │   └─ loading       - 加载状态
    │
    ├─ social.ts         - 社交状态
    │   ├─ followingList - 关注列表
    │   ├─ followersList - 粉丝列表
    │   ├─ comments      - 评论列表
    │   └─ commentTotal  - 评论总数
    │
    └─ folder.ts         - 文件夹状态
        ├─ folders       - 文件夹列表
        ├─ currentFolderId - 当前文件夹ID
        └─ loading       - 加载状态
```

---

## 🎯 核心功能模块详解

### 1. 用户认证系统

#### 登录方式

支持三种登录方式：

| 方式           | 字段    | 验证规则                   |
| -------------- | ------- | -------------------------- |
| 用户名登录     | account | 3-20字符，字母/数字/下划线 |
| 邮箱登录       | account | 有效邮箱格式，最大50字符   |
| 手机验证码登录 | phone   | 11位手机号 + 6位验证码     |

#### 密码规则

- 长度：8-20个字符
- 必须包含：大写字母、小写字母、数字、特殊字符(!@#$%^&\*等)

#### 登录弹窗交互

- **弹窗化设计**：不跳转页面，全局弹窗登录
- **左右分栏布局**：
  - 左侧：品牌信息（Logo、标题、描述）
  - 右侧：表单区域
- **模式切换**：登录/注册无缝切换
- **登录方式切换**：账号密码 ⇄ 手机验证码

#### 核心文件

- `src/components/AuthModal.vue` - 登录/注册弹窗
- `src/store/modules/user.ts` - 用户状态管理
- `src/api/user.ts` - 用户 API
- `src/api/types/user.ts` - 用户类型定义

#### 类型定义

```typescript
// 登录参数
export interface LoginParams {
  account: string // 用户名或邮箱
  password: string
}

// 登录响应
export interface LoginResponse {
  token: string
  user: UserInfo
}

// 用户信息
export interface UserInfo {
  id?: number
  username: string
  email?: string
  avatar?: string
}

// 用户详情（包含社交信息）
export interface UserDetail {
  id: string
  username: string
  nickname: string
  avatar: string
  bio?: string
  followingCount: number
  followersCount: number
  likeCount: number
  isFollowing?: boolean
}
```

### 2. 笔记管理系统

#### 笔记数据结构

```typescript
// 笔记接口
export interface Note {
  id: string
  title: string
  content: string // Markdown格式
  coverMedia: MediaItem[] // 封面媒体
  images: string[] // 文中图片列表
  authorId: string
  authorName: string
  authorAvatar: string
  visibility: NoteVisibility // 'public' | 'private'
  likeCount: number
  collectCount: number
  commentCount: number
  viewCount: number
  isLiked?: boolean
  isCollected?: boolean
  createdAt: string
  updatedAt: string
}

// 媒体项
export interface MediaItem {
  type: 'image' | 'video'
  url: string
  width: number
  height: number
  duration?: number // 视频时长（秒）
}
```

#### 笔记排序类型

```typescript
export type NoteSortType = 'latest' | 'hot' | 'recommend'
```

#### 笔记 Store Actions

| Action                | 说明         |
| --------------------- | ------------ |
| `fetchNotes(params)`  | 获取笔记列表 |
| `loadMore()`          | 加载更多     |
| `preloadNext()`       | 预加载下一页 |
| `fetchNoteDetail(id)` | 获取笔记详情 |
| `createNote(params)`  | 创建笔记     |
| `updateNote(params)`  | 更新笔记     |
| `deleteNote(id)`      | 删除笔记     |
| `likeNote(id)`        | 点赞笔记     |
| `collectNote(id)`     | 收藏笔记     |

### 3. 社交互动系统

#### 关注系统

```typescript
// 关注用户
followUser(userId: string): Promise<boolean>

// 取消关注
unfollowUser(userId: string): Promise<boolean>

// 获取关注列表
fetchFollowingList(userId?: string): Promise<void>

// 获取粉丝列表
fetchFollowersList(userId?: string): Promise<void>
```

#### 评论系统

```typescript
// 评论数据结构
export interface Comment {
  id: string
  noteId: string
  userId: string
  username: string
  userAvatar: string
  content: string
  likeCount: number
  isLiked?: boolean
  replyTo?: string // 回复的评论ID
  replyToUser?: string // 回复的用户名
  createdAt: string
  replies?: Comment[] // 子回复列表
  replyCount?: number
}

// 评论点赞响应
export interface LikeCommentResponse {
  code: number
  message: string
  data: {
    isLiked: boolean
    likeCount: number
  }
}
```

#### 评论 Store Actions

| Action                                 | 说明              |
| -------------------------------------- | ----------------- |
| `fetchComments(noteId, page)`          | 获取评论列表      |
| `addComment(noteId, content, replyTo)` | 添加评论          |
| `likeComment(commentId)`               | 评论点赞/取消点赞 |

#### 评论点赞功能

- **图标**：心形 SVG（支持填充/空心切换）
- **状态同步**：使用接口返回的 `isLiked` 和 `likeCount`
- **递归更新**：支持更新主评论和嵌套回复的点赞状态

### 4. 多媒体封面系统

#### 封面设置方式

1. **从文中选择**：正则提取 Markdown 图片，弹窗网格多选
2. **本地上传**：读取图片/视频尺寸元数据，立即上传到 COS
3. **不设置**：自动提取文中前 3 张图片

#### 展示逻辑

```typescript
// 智能获取封面
export function getDisplayCoverMedia(
  coverMedia: MediaItem[] | undefined,
  content: string,
  limit: number = 3,
): MediaItem[] {
  // 优先使用手动设置的封面
  if (coverMedia && coverMedia.length > 0) {
    return coverMedia
  }
  // 否则从内容中提取图片
  const imageUrls = extractImagesFromMarkdown(content)
  return imageUrls.slice(0, limit).map((url) => ({
    type: 'image',
    url,
    width: 800,
    height: 600,
  }))
}
```

#### 列表页与详情页差异

- **列表页**：只展示第一张封面
- **详情页**：展示完整轮播
- **视频封面**：列表页右上角显示播放图标

### 5. Markdown 编辑器

#### 功能特性

- 图标化工具栏（B/I/S字符 + Element Plus 图标）
- 无边框沉浸式输入体验
- 8 分类表情选择器
- 实时预览模式（分屏显示）
- 图片上传插入
- Markdown 文件导入

#### 工具栏按钮

| 按钮 | 功能   | 快捷键 |
| ---- | ------ | ------ |
| B    | 加粗   | Ctrl+B |
| I    | 斜体   | Ctrl+I |
| S    | 删除线 | -      |
| H    | 标题   | -      |
| 📷   | 图片   | -      |
| 😊   | 表情   | -      |
| 👁   | 预览   | -      |

### 6. 视图模式

#### 首页布局

```
┌─────────────────────────────────────┐
│ 关注动态区（走马灯卡片）             │
│ ← [卡片1] [卡片2] [卡片3] →         │
├─────────────────────────────────────┤
│ 排序标签：⭐推荐 🕐最新 🏆最热       │
├─────────────────────────────────────┤
│ 瀑布流内容区                         │
│ ┌────┐ ┌────┐ ┌────┐               │
│ │    │ │    │ │    │               │
│ │    │ │    │ │    │               │
│ └────┘ └────┘ └────┘               │
│ ┌────┐ ┌────┐ ┌────┐               │
│ │    │ │    │ │    │               │
│ └────┘ └────┘ └────┘               │
└─────────────────────────────────────┘
```

#### 瀑布流特性

- 支持预加载（滚动到底部前 300px 触发）
- 双尺寸卡片布局（竖版 4:3，横版 16:9）
- 隐藏滚动条但保持滚动功能

---

## 🔧 构建配置

### Vite 配置

```typescript
// vite.config.ts
export default defineConfig({
  plugins: [
    vue(),
    vueDevTools(),
    AutoImport({
      imports: ['vue', 'vue-router'],
      dts: 'auto-imports.d.ts',
      resolvers: [ElementPlusResolver()],
    }),
    Components({
      resolvers: [ElementPlusResolver()],
    }),
  ],
  css: {
    preprocessorOptions: {
      scss: {
        additionalData: (content, filePath) => {
          if (filePath.includes('assets/styles/index.scss')) {
            return content
          }
          return `@use "sass:color";\n@use "@/assets/styles" as *;\n${content}`
        },
      },
    },
  },
  resolve: {
    alias: { '@': fileURLToPath(new URL('./src', import.meta.url)) },
  },
})
```

### NPM Scripts

| 命令                 | 说明            |
| -------------------- | --------------- |
| `npm run dev`        | 启动开发服务器  |
| `npm run build:dev`  | 构建开发环境    |
| `npm run build:prod` | 构建生产环境    |
| `npm run preview`    | 预览构建结果    |
| `npm run type-check` | 类型检查        |
| `npm run lint`       | ESLint 检查修复 |
| `npm run format`     | Prettier 格式化 |

---

## 🌐 API 接口设计

### 用户模块

| 接口                  | 方法 | 说明             |
| --------------------- | ---- | ---------------- |
| `/api/users/login`    | POST | 用户登录         |
| `/api/users/register` | POST | 用户注册         |
| `/api/users/info`     | GET  | 获取当前用户信息 |
| `/api/users/:userId`  | GET  | 获取用户详情     |

### 笔记模块

| 接口                     | 方法   | 说明          |
| ------------------------ | ------ | ------------- |
| `/api/notes`             | GET    | 获取笔记列表  |
| `/api/notes`             | POST   | 创建笔记      |
| `/api/notes/:id`         | GET    | 获取笔记详情  |
| `/api/notes/:id`         | PUT    | 更新笔记      |
| `/api/notes/:id`         | DELETE | 删除笔记      |
| `/api/notes/:id/like`    | POST   | 点赞/取消点赞 |
| `/api/notes/:id/collect` | POST   | 收藏/取消收藏 |

### 社交模块

| 接口                        | 方法 | 说明              |
| --------------------------- | ---- | ----------------- |
| `/api/social/follow`        | POST | 关注/取消关注用户 |
| `/api/social/following`     | GET  | 获取关注列表      |
| `/api/social/followers`     | GET  | 获取粉丝列表      |
| `/api/social/comments`      | GET  | 获取评论列表      |
| `/api/social/comments`      | POST | 添加评论          |
| `/api/social/comments/like` | POST | 评论点赞/取消点赞 |

### 文件夹模块

| 接口               | 方法   | 说明           |
| ------------------ | ------ | -------------- |
| `/api/folders`     | GET    | 获取文件夹列表 |
| `/api/folders`     | POST   | 创建文件夹     |
| `/api/folders/:id` | PUT    | 更新文件夹     |
| `/api/folders/:id` | DELETE | 删除文件夹     |

---

## 🔒 请求拦截与错误处理

### Axios 封装

```typescript
// src/utils/request.ts
const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 5000,
})

// 请求拦截器：自动携带 token
request.interceptors.request.use((config) => {
  const userStore = useUserStore()
  if (userStore.token) {
    config.headers.Authorization = `Bearer ${userStore.token}`
  }
  return config
})

// 响应拦截器：统一错误处理
request.interceptors.response.use(
  (response) => response.data,
  (error) => {
    const status = error.response?.status
    const backendMessage = error.response?.data?.message

    // 401: token过期，自动登出并弹出登录框
    if (status === 401) {
      userStore.logout()
      userStore.openLoginModal()
    }

    ElMessage.error(backendMessage || getDefaultMessage(status))
    return Promise.reject(error)
  },
)
```

### 错误处理规范

- **透传原则**：业务层不重复调用 `ElMessage.error`，由拦截器统一处理
- **优先后端消息**：使用后端返回的 `message` 字段
- **默认提示**：网络错误、服务器错误等使用默认提示

---

## 🎨 样式规范

### 设计变量

```scss
// 主题色 - 可爱生动绿色系
$primary-color: #6dba7a;
$primary-light: #8dd399;
$primary-gradient: linear-gradient(135deg, #6dba7a 0%, #8dd399 100%);

// 中性色
$text-primary: #3a4a3e;
$text-secondary: #5d6e62;
$text-muted: #91a599;
$border-color: #d6e3da;
$background-color: #f2f6f3;

// 字体大小
$font-xs: 12px;
$font-sm: 13px;
$font-base: 14px;
$font-lg: 16px;
$font-xl: 18px;

// 间距
$spacing-xs: 4px;
$spacing-sm: 8px;
$spacing-md: 16px;
$spacing-lg: 24px;
$spacing-xl: 32px;

// 圆角
$radius-sm: 4px;
$radius-md: 8px;
$radius-lg: 12px;
$radius-xl: 16px;
```

---

## 📝 核心代码示例

### 1. 登录弹窗组件

```vue
<template>
  <el-dialog v-model="userStore.authModalVisible" class="auth-dialog">
    <div class="auth-card">
      <!-- 左侧品牌信息 -->
      <div class="auth-left">
        <div class="brand-info">
          <h1>旅行笔记</h1>
          <p>记录旅途中的每一个精彩瞬间</p>
        </div>
      </div>

      <!-- 右侧表单区 -->
      <div class="auth-right">
        <!-- 登录表单 / 注册表单 -->
        <el-form>...</el-form>
      </div>
    </div>
  </el-dialog>
</template>
```

### 2. 评论点赞

```vue
<template>
  <span class="action-btn" @click="handleLike(comment.id)">
    <el-icon>
      <svg viewBox="0 0 1024 1024" :class="{ liked: comment.isLiked }">
        <path
          :fill="comment.isLiked ? 'currentColor' : 'none'"
          stroke="currentColor"
          stroke-width="40"
          d="M512 896a42.666667 42.666667 0 0 1-30.293333-12.373333l-331.093334-324.266667c-73.386667-73.386667-73.386667-192.853333 0-266.24 73.386667-73.386667 192.853333-73.386667 266.24 0L512 388.48l95.146667-95.36c73.386667-73.386667 192.853333-73.386667 266.24 0 73.386667 73.386667 73.386667 192.853333 0 266.24l-331.093334 324.266667A42.666667 42.666667 0 0 1 512 896z"
        />
      </svg>
    </el-icon>
    {{ comment.likeCount }}
  </span>
</template>
```

### 3. 评论点赞 Store

```typescript
// 评论点赞/取消点赞
async likeComment(commentId: string) {
  const response = await socialApi.likeComment({ commentId })
  if (response.code === 200) {
    const { isLiked, likeCount } = response.data
    // 递归更新评论状态
    const updateCommentLike = (comments: Comment[]): boolean => {
      for (const comment of comments) {
        if (comment.id === commentId) {
          comment.isLiked = isLiked
          comment.likeCount = likeCount
          return true
        }
        if (comment.replies && updateCommentLike(comment.replies)) {
          return true
        }
      }
      return false
    }
    updateCommentLike(this.comments)
  }
}
```

---

## 📦 依赖清单

```json
{
  "dependencies": {
    "axios": "^1.13.2",
    "element-plus": "^2.13.0",
    "pinia": "^3.0.4",
    "vue": "^3.5.22",
    "vue-router": "^4.6.4",
    "vuedraggable": "^4.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@tsconfig/node22": "^22.0.2",
    "@types/node": "^22.18.11",
    "@vitejs/plugin-vue": "^6.0.1",
    "@vue/eslint-config-prettier": "^10.2.0",
    "@vue/eslint-config-typescript": "^14.6.0",
    "@vue/tsconfig": "^0.8.1",
    "prettier": "3.6.2",
    "sass-embedded": "^1.97.2",
    "typescript": "~5.9.0",
    "unplugin-auto-import": "^20.3.0",
    "unplugin-vue-components": "^30.0.0",
    "vite": "^7.1.11",
    "vite-plugin-vue-devtools": "^8.0.3",
    "vue-tsc": "^3.1.1"
  }
}
```

---

## 🧪 测试要点

### 功能测试

- [ ] 用户注册（用户名、邮箱、密码验证）
- [ ] 用户登录（用户名/邮箱双模式）
- [ ] 手机验证码登录
- [ ] 笔记创建（标题、内容、封面设置）
- [ ] 笔记编辑和删除
- [ ] 笔记点赞和收藏
- [ ] 关注/取消关注用户
- [ ] 发表评论
- [ ] 评论点赞（心形图标切换）
- [ ] 瀑布流滚动加载

### 性能测试

- [ ] 大量图片上传处理
- [ ] 瀑布流预加载性能
- [ ] 内存泄漏检测

---

**文档维护**：本文档将随项目迭代持续更新，记录所有设计决策和实施细节。

**变更记录**：

- 2026-03-03：
  - 更新技术栈版本信息
  - 新增评论点赞功能文档
  - 新增完整的 API 接口设计
  - 新增 Axios 拦截器与错误处理规范
  - 新增样式变量说明
  - 完善用户认证系统文档
  - 添加项目目录结构说明
  - 优化文档结构和可读性

- 2026-02-26：
  - 卸载未使用的依赖包
  - 实现自定义视频播放器
  - 列表接口优化：只返回第一张封面
  - 瀑布流双尺寸卡片布局
  - 创建页改造为公众号风格
  - MarkdownEditor工具栏图标化
